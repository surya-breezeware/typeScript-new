// never will not return any value ,never can not have an end point

let func = (num1: number): void => {
  const result = num1;

  // while (true) {
  console.log(result);
  // }
};

console.log(func(25));

export {};
